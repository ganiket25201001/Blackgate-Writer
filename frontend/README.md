Blackgate Writer — Frontend (Step 1)

Commands:

Install dependencies:

```bash
cd frontend
npm install
```

Run dev server:

```bash
npm run dev
```

Notes:
- Tailwind dark theme is enabled via `dark` class on `html`.
- Layout components live in `src/components`.
- Tiptap integration will be added in a later step.
